"""Shared constants for KAgent ADK integration."""

X_FOO_BAR_HEADER_NAME = "x-foo-bar"
"""HTTP header name carrying the foo-bar correlation token from clients."""

X_FOO_BAR_SESSION_STATE_KEY = "kagent:x-foo-bar"
"""Session state key used to persist the foo-bar token across agent runs."""
